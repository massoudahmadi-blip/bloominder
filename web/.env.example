# Copy to web/.env.local

# Bloominder API base URL. Leave EMPTY to run on built-in mock data (great for design work
# before the VPS exists). Set it once your API is live, e.g. https://api.bloominder.com
NEXT_PUBLIC_API_URL=
